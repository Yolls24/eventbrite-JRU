<h1>Liste des événements</h1>

<%= link_to "Nouvel événement", new_event_path, class: "btn btn-primary" %>

<ul>
  <% @events.each do |event| %>
    <li>
      <%= link_to event.title, event_path(event) %>
      (Le <%= event.start_date.strftime("%d %b %Y %H:%M") %>)
    </li>
  <% end %>
</ul>

